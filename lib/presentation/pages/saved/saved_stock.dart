import 'package:flutter/material.dart';

class SavedStockData extends StatelessWidget {
  const SavedStockData({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}